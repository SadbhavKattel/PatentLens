# Additional dependencies for notebooks/02_model_training.ipynb and app.py,
# on top of whatever's already in requirements.txt.
# (requirements.txt is a raw `pip freeze` snapshot in UTF-16 and shouldn't be hand-edited.)

faiss-cpu
nltk
streamlit
altair
joblib
pyarrow

# Only needed if you scale up the corpus via src/patentlens/data_fetch.py (BigQuery):
google-cloud-bigquery
db-dtypes
